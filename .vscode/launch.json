{
    "configurations": [
        {
            "name": "Launch Chrome",
            "request": "launch",
            "type": "pwa-chrome",
            "url": "http://localhost:4000",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
